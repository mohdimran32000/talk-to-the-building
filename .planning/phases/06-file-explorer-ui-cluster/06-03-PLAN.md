---
phase: 06-file-explorer-ui-cluster
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ui/context-menu.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/alert-dialog.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/separator.tsx
autonomous: true
requirements: [UI-04, UI-06, UI-08]
tags: [phase6, frontend, deps, wave0]
must_haves:
  truths:
    - "@dnd-kit/core@6.3.1 is installed and importable"
    - "@dnd-kit/sortable@10.0.0 is installed and importable"
    - "@radix-ui/react-context-menu is installed (transitively via shadcn)"
    - "Six new shadcn primitives exist under frontend/src/components/ui/"
    - "Existing TypeScript build still passes after install (no version conflicts)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependency declarations for the Phase 6 UI stack"
      contains: "@dnd-kit/core"
    - path: "frontend/src/components/ui/context-menu.tsx"
      provides: "shadcn ContextMenu primitive backing UI-04 right-click CRUD"
    - path: "frontend/src/components/ui/alert-dialog.tsx"
      provides: "shadcn AlertDialog primitive backing UI-04 (delete confirm) and UI-06 (cross-scope BLOCK modal)"
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive backing folder-create form"
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive backing scope + status badges (UI-08)"
    - path: "frontend/src/components/ui/tooltip.tsx"
      provides: "shadcn Tooltip primitive for hover hints"
    - path: "frontend/src/components/ui/separator.tsx"
      provides: "shadcn Separator primitive for section dividers"
  key_links:
    - from: "frontend/package.json"
      to: "node_modules/@dnd-kit/core"
      via: "npm install"
      pattern: "@dnd-kit/core"
---

<objective>
Install the three new runtime deps (`@dnd-kit/core@^6.3.1`, `@dnd-kit/sortable@^10.0.0`, `@radix-ui/react-context-menu@^2.2.16` — verified versions per CONTEXT.md and RESEARCH.md on 2026-05-10) and add the six shadcn UI primitives Phase 6 needs (`context-menu`, `dialog`, `alert-dialog`, `badge`, `tooltip`, `separator`) via the shadcn CLI.

Purpose: Wave-0 foundation. Plans 06-06 / 06-09 / 06-10 cannot import these primitives until they exist on disk. Splitting deps into their own plan keeps subsequent plans focused on logic, not setup.
Output: Updated `package.json` with three new entries, new `package-lock.json`, and six new files under `frontend/src/components/ui/`.
</objective>

<execution_context>
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/workflows/execute-plan.md
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-file-explorer-ui-cluster/06-CONTEXT.md
@.planning/phases/06-file-explorer-ui-cluster/06-RESEARCH.md
@.planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing dependencies block in frontend/package.json:12-28 -->
```json
"dependencies": {
  "@supabase/supabase-js": "^2.95.3",
  "@tailwindcss/vite": "^4.1.18",
  "class-variance-authority": "^0.7.1",
  "clsx": "^2.1.1",
  "lucide-react": "^0.563.0",
  "next-themes": "^0.4.6",
  "radix-ui": "^1.4.3",
  "react": "^19.2.0",
  ...
}
```

<!-- Existing shadcn primitives in frontend/src/components/ui/ -->
button.tsx, card.tsx, input.tsx, label.tsx, sonner.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false" id="06-03-01">
  <name>Task 1: Install @dnd-kit/core + @dnd-kit/sortable</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (entire file — existing deps + scripts to confirm install command shape)
    - .planning/phases/06-file-explorer-ui-cluster/06-CONTEXT.md (D-01, "Drag-and-drop" decision — version pins are LOCKED per researcher verification on 2026-05-10)
  </read_first>
  <action>
From the `frontend/` directory (use absolute path: `C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/frontend`), run:

```bash
npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 --save-exact
```

Notes:
- `--save-exact` pins to the exact verified versions per CONTEXT.md "Drag-and-drop" decision. Do NOT use `^` or `~` here — researcher verified 6.3.1 + 10.0.0 are React-19 compatible; we don't want a minor drift to break things.
- After install, the `package.json` `dependencies` block must contain BOTH `"@dnd-kit/core": "6.3.1"` AND `"@dnd-kit/sortable": "10.0.0"` (no caret). Verify by reading the file post-install.
- `package-lock.json` will update automatically.

If the install fails with peer-dep complaints involving React 19, retry with `npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 --save-exact --legacy-peer-deps` and document the flag in the SUMMARY.

Do NOT run `npm install` without arguments first; only the targeted dep install.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); if(!p.dependencies['@dnd-kit/core']) throw new Error('missing @dnd-kit/core'); if(!p.dependencies['@dnd-kit/sortable']) throw new Error('missing @dnd-kit/sortable'); console.log('OK:', p.dependencies['@dnd-kit/core'], p.dependencies['@dnd-kit/sortable'])"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./frontend/node_modules/@dnd-kit/core/package.json').version)"` prints `6.3.1`.
    - `node -e "console.log(require('./frontend/node_modules/@dnd-kit/sortable/package.json').version)"` prints `10.0.0`.
    - `frontend/package.json` `dependencies` block contains `"@dnd-kit/core": "6.3.1"` and `"@dnd-kit/sortable": "10.0.0"` (exact, no `^`).
    - `cd frontend && npx tsc --noEmit` exits 0 (existing build still type-checks — no version conflict broke anything).
  </acceptance_criteria>
  <done>
    Both dnd-kit packages installed at the exact pinned versions; existing build is unaffected.
  </done>
</task>

<task type="auto" tdd="false" id="06-03-02">
  <name>Task 2: Install six shadcn primitives via CLI</name>
  <files>frontend/src/components/ui/context-menu.tsx, frontend/src/components/ui/dialog.tsx, frontend/src/components/ui/alert-dialog.tsx, frontend/src/components/ui/badge.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/separator.tsx, frontend/package.json</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (existing CLI-installed shadcn output — confirms the convention: never hand-edit; CLI generates it)
    - frontend/src/components/ui/card.tsx (same — second confirmation)
    - .planning/phases/06-file-explorer-ui-cluster/06-RESEARCH.md (lines 449-466 — "shadcn primitives needed" table; CLI command is verbatim)
    - .planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md (lines 597-606 — install command with version pin)
  </read_first>
  <action>
From the `frontend/` directory, run the shadcn CLI:

```bash
npx shadcn@3.8.4 add context-menu dialog alert-dialog badge tooltip separator
```

The CLI is interactive on the FIRST run. To avoid the prompt, pass `--yes`:

```bash
npx shadcn@3.8.4 add context-menu dialog alert-dialog badge tooltip separator --yes
```

If `npx shadcn@3.8.4` is rejected by the CLI (the `add` subcommand may have moved to the root in newer versions), fallback in priority order:
1. `npx shadcn@latest add context-menu dialog alert-dialog badge tooltip separator --yes`
2. `npx shadcn-ui@latest add context-menu dialog alert-dialog badge tooltip separator --yes`

Document the working command in the SUMMARY.

Expected outputs (the CLI creates these files automatically):
- `frontend/src/components/ui/context-menu.tsx` (new)
- `frontend/src/components/ui/dialog.tsx` (new)
- `frontend/src/components/ui/alert-dialog.tsx` (new)
- `frontend/src/components/ui/badge.tsx` (new)
- `frontend/src/components/ui/tooltip.tsx` (new)
- `frontend/src/components/ui/separator.tsx` (new)

The CLI also adds these transitive Radix subpackages to `package.json`:
- `@radix-ui/react-context-menu` (UI-04)
- `@radix-ui/react-dialog`
- `@radix-ui/react-alert-dialog`
- `@radix-ui/react-tooltip`
- `@radix-ui/react-separator`

Do NOT hand-edit any generated `ui/*.tsx` file (matches the existing convention — `button.tsx` and `card.tsx` are unmodified CLI output).

If the CLI prompts about `tailwind.config` or `components.json`, accept the existing config (the project already has shadcn set up — see `frontend/components.json` if present).

After install, verify all six files exist and the new Radix subpackages are in `package.json` dependencies.
  </action>
  <verify>
    <automated>cd frontend && for f in context-menu dialog alert-dialog badge tooltip separator; do test -f "src/components/ui/$f.tsx" || { echo "MISSING: src/components/ui/$f.tsx"; exit 1; }; done && node -e "const p=require('./package.json'); ['@radix-ui/react-context-menu','@radix-ui/react-dialog','@radix-ui/react-alert-dialog'].forEach(d=>{ if(!p.dependencies[d]) throw new Error('missing dep '+d); }); console.log('OK: 6 primitives + radix subpackages installed')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist: `frontend/src/components/ui/{context-menu,dialog,alert-dialog,badge,tooltip,separator}.tsx`.
    - `frontend/package.json` `dependencies` includes `@radix-ui/react-context-menu`, `@radix-ui/react-dialog`, `@radix-ui/react-alert-dialog`, `@radix-ui/react-tooltip`, `@radix-ui/react-separator` (transitive shadcn install).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - None of the generated `ui/*.tsx` files were hand-edited after CLI generation (`git diff` should show only insertions where the CLI wrote the files).
    - `cd frontend && grep -l "ContextMenu" src/components/ui/context-menu.tsx | head -1` returns the path (file actually contains the named export).
  </acceptance_criteria>
  <done>
    Six shadcn primitives are on disk under `frontend/src/components/ui/`, transitive Radix deps are in `package.json`, and `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (no version conflicts introduced)
- `cd frontend && npm ls @dnd-kit/core @dnd-kit/sortable @radix-ui/react-context-menu @radix-ui/react-dialog @radix-ui/react-alert-dialog` reports no `UNMET DEPENDENCY` and no version mismatches
- `cd frontend && ls src/components/ui/*.tsx | wc -l` is at least 11 (5 existing + 6 new)
</verification>

<success_criteria>
Plans 06-06 (FolderNode tree primitives), 06-09 (ContextMenu CRUD UI), and 06-10 (dnd-kit drag-and-drop wiring) can `import` from the new packages and shadcn primitives without runtime or type errors. The frontend build remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-explorer-ui-cluster/06-03-SUMMARY.md` documenting:
- Exact npm install command(s) that succeeded (incl. any `--legacy-peer-deps` fallback)
- Exact shadcn CLI command that succeeded
- The six new file paths
- Final pinned versions of dnd-kit packages
- Output of `cd frontend && npx tsc --noEmit` (should be empty / exit 0)
</output>
