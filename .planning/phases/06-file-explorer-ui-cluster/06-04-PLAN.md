---
phase: 06-file-explorer-ui-cluster
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/app/routers/messages.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [UI-10]
tags: [phase6, sse, cleanup, wave0]
must_haves:
  truths:
    - "backend/app/routers/messages.py emits ZERO `\"type\": \"sub_agent_*\"` envelopes (legacy with trailing underscore)"
    - "backend/app/routers/messages.py STILL emits the generalized `{\"type\": \"sub_agent\", agent_name, event, payload}` envelopes on all 5 sub-agent SSE arms"
    - "frontend/src/lib/api.ts has ZERO `parsed.type === 'sub_agent_*'` legacy branches"
    - "frontend/src/lib/api.ts has ONE generalized `parsed.type === 'sub_agent'` branch with switch on parsed.event"
    - "Phase 5 dual-emit window is closed in this commit per locked plan-checker hook in 05-04-SUMMARY.md"
  artifacts:
    - path: "backend/app/routers/messages.py"
      provides: "SSE generator with ONLY the generalized sub-agent envelope"
      contains: "type\": \"sub_agent\""
    - path: "frontend/src/lib/api.ts"
      provides: "SSE consumer with ONE generalized sub_agent branch and ZERO legacy sub_agent_* branches"
      contains: "parsed.type === 'sub_agent'"
  key_links:
    - from: "backend/app/routers/messages.py:event_generator"
      to: "frontend/src/lib/api.ts:sendMessage"
      via: "SSE generalized envelope"
      pattern: "\"type\": \"sub_agent\""
---

<objective>
Close the Phase 5 dual-emit window: delete the 5 legacy `yield json.dumps({"type": "sub_agent_*", ...})` lines in `backend/app/routers/messages.py` and the 5 legacy `parsed.type === 'sub_agent_*'` branches in `frontend/src/lib/api.ts`. Replace the latter with ONE generalized `parsed.type === 'sub_agent'` branch that switches on `parsed.event` (start | token | tool_start | tool_done | done).

Purpose: This is the locked Phase 6 plan-checker hook from `.planning/phases/05-explorer-sub-agent-sse-protocol-generalization/05-04-SUMMARY.md` "Next Phase Readiness". The dual-emit window is intentionally short — leaving it open across phases pays the protocol-fork tax forever (Pitfall 12). Doing this in Wave 0 ensures every subsequent Phase 6 frontend plan consumes the canonical envelope.
Output:
- `backend/app/routers/messages.py`: 5 legacy `yield` lines deleted; the 5 generalized `yield` lines preserved bit-identically; comments referencing "dual-emit / Phase 6 cleanup" removed.
- `frontend/src/lib/api.ts`: 5 legacy SSE branches deleted; 1 generalized branch added (per RESEARCH.md §SSE envelope switchover lines 642-650 — verbatim).
</objective>

<execution_context>
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/workflows/execute-plan.md
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-file-explorer-ui-cluster/06-CONTEXT.md
@.planning/phases/06-file-explorer-ui-cluster/06-RESEARCH.md
@.planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md
@.planning/phases/05-explorer-sub-agent-sse-protocol-generalization/05-04-SUMMARY.md
@.planning/phases/05-explorer-sub-agent-sse-protocol-generalization/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false" id="06-04-01">
  <name>Task 1: Delete 5 legacy yield lines in backend/app/routers/messages.py</name>
  <files>backend/app/routers/messages.py</files>
  <read_first>
    - backend/app/routers/messages.py (lines 100-210 — the entire event_generator with ALL five SSE arms; you MUST see the dual-emit pattern with both legacy + generalized yields side-by-side)
    - .planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md (lines 436-453 — exact line numbers and verbatim text of each line to delete)
    - .planning/phases/05-explorer-sub-agent-sse-protocol-generalization/05-04-SUMMARY.md ("Next Phase Readiness" section — confirms this is the locked Phase 6 cleanup hook)
  </read_first>
  <action>
Open `backend/app/routers/messages.py`. Locate the `event_generator` function. It currently has FIVE dual-emit pairs (one per sub-agent SSE event). For EACH pair, KEEP the generalized `yield json.dumps({"type": "sub_agent", ...})` line and DELETE the legacy `yield json.dumps({"type": "sub_agent_*", ...})` line.

Verbatim deletion targets per PATTERNS.md (line numbers approximate — Phase 5 commit may have shifted them; use the exact text strings to locate):

| Approx Line | Verbatim text to DELETE |
|-------------|---------------------------|
| ~120 | `yield json.dumps({"type": "sub_agent_start", **parsed})` |
| ~141 | `yield json.dumps({"type": "sub_agent_tool_start", **parsed})` |
| ~160 | `yield json.dumps({"type": "sub_agent_tool_done", **parsed})` |
| ~176 | `yield json.dumps({"type": "sub_agent_token", "content": data})` |
| ~198 | `yield json.dumps({"type": "sub_agent_done"})` |

Also delete the surrounding dual-emit scaffolding comments that reference the now-closed window:
- The header comment "Dual-emit window (Phase 5 ONLY — removed in Phase 6 cleanup):" (typically just before the first legacy yield)
- The inline comments "1) LEGACY shape" / "2) GENERALIZED envelope" tags throughout the five arms

Do NOT touch:
- The five `yield json.dumps({"type": "sub_agent", "agent_name": ..., "event": ..., "payload": ...})` lines — they STAY.
- The `tool_metadata` accumulator code at lines ~111-123 — UNCHANGED per Phase 5 lock.
- Any non-sub-agent yield (regular `token` events, `done` events for the main agent, etc.).

After edits, verify the surrounding control flow still parses (Python file is valid). Re-read your changes to confirm you only deleted the 5 legacy lines + their dual-emit scaffolding comments, and not any of the generalized envelope yields.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import ast; ast.parse(open('app/routers/messages.py').read()); print('OK: messages.py parses')" && grep -c '"type": "sub_agent_' backend/app/routers/messages.py | (read n; if [ "$n" != "0" ]; then echo "FAIL: $n legacy sub_agent_* literals still present"; exit 1; else echo "OK: zero legacy sub_agent_* literals"; fi)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"type": "sub_agent_' backend/app/routers/messages.py` returns `0` (NO `sub_agent_` with trailing underscore — the legacy shape).
    - `grep -c '"type": "sub_agent"' backend/app/routers/messages.py` returns `5` (the 5 generalized envelopes — `sub_agent` WITHOUT trailing underscore — are still present, one per SSE arm).
    - `cd backend && venv/Scripts/python -c "import ast; ast.parse(open('app/routers/messages.py').read())"` exits 0 (file is valid Python).
    - `grep -i "dual.emit\|Phase 6 cleanup" backend/app/routers/messages.py` returns NO matches (cleanup comments removed).
    - `grep -n "tool_metadata" backend/app/routers/messages.py` still returns multiple matches (the persistence path is UNCHANGED per Phase 5 lock).
    - `git diff --stat backend/app/routers/messages.py` shows ONLY deletions (zero additions) for this file in this task — pure subtraction.
  </acceptance_criteria>
  <done>
    Backend SSE emitter speaks ONLY the generalized envelope. Legacy emissions deleted; generalized emissions and tool_metadata accumulator UNCHANGED.
  </done>
</task>

<task type="auto" tdd="false" id="06-04-02">
  <name>Task 2: Replace 5 legacy SSE branches with 1 generalized branch in api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (lines 280-350 — the SSE consumer block; you MUST see the FIVE existing `parsed.type === 'sub_agent_*'` branches and the surrounding else-if chain)
    - .planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md (lines 411-431 — exact verbatim of legacy branches to delete + generalized branch to insert)
    - .planning/phases/06-file-explorer-ui-cluster/06-RESEARCH.md (lines 638-651 — RESEARCH.md sketch of the new branch shape)
  </read_first>
  <action>
Open `frontend/src/lib/api.ts`. Locate the SSE event-handling block (typically around lines 280-330). It currently contains FIVE legacy branches (verbatim, per PATTERNS.md):

```ts
} else if (parsed.type === 'sub_agent_start') { ... }       // ~line 306
} else if (parsed.type === 'sub_agent_token') { ... }       // ~line 311
} else if (parsed.type === 'sub_agent_tool_start') { ... }  // ~line 313
} else if (parsed.type === 'sub_agent_tool_done') { ... }   // ~line 316
} else if (parsed.type === 'sub_agent_done') { ... }        // ~line 319
```

DELETE all 5. INSERT in their place ONE generalized branch (verbatim from RESEARCH.md §SSE envelope switchover, adapted to project's existing callback names):

```ts
} else if (parsed.type === 'sub_agent') {
  switch (parsed.event) {
    case 'start':
      onSubAgentStart?.({ ...parsed.payload, agent_name: parsed.agent_name })
      break
    case 'token':
      onSubAgentToken?.(parsed.payload.content)
      break
    case 'tool_start':
      onSubAgentToolStart?.(parsed.payload)
      break
    case 'tool_done':
      onSubAgentToolDone?.(parsed.payload)
      break
    case 'done':
      onSubAgentDone?.()
      break
  }
}
```

Notes on callback names:
- Re-use the EXISTING `sendMessage()` callback parameter names — Phase 5 / Plan 05-05 already wired them. DO NOT rename or change the signature of `sendMessage()` in this task. If the existing callback names differ from the above (e.g., `onSubAgentStart` may be called `onSubAgentBegin`), match the existing name verbatim — read lines 200-280 of `api.ts` to confirm the actual parameter names before writing the branch.
- Preserve the `?.` optional-chaining pattern — callers may not pass every callback.
- The `agent_name` is at `parsed.agent_name` (top-level), not inside `parsed.payload` — the backend wraps it that way. Pass it into `onSubAgentStart` by spreading the payload then adding `agent_name` (per the verbatim sketch above).

Do NOT change:
- The other `else if` branches (regular `token`, main-agent `tool_*`, `done`, error handling — all UNCHANGED).
- The `sendMessage()` function signature or its callback parameter list.
- Any export shape — this is purely internal logic replacement.

After the edit, the file should have ONE `parsed.type === 'sub_agent'` branch and ZERO `parsed.type === 'sub_agent_*'` branches.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c "parsed.type === 'sub_agent_" frontend/src/lib/api.ts | (read n; if [ "$n" != "0" ]; then echo "FAIL: $n legacy sub_agent_* branches still present"; exit 1; else echo "OK: zero legacy branches"; fi) && grep -c "parsed.type === 'sub_agent'" frontend/src/lib/api.ts | (read n; if [ "$n" != "1" ]; then echo "FAIL: expected exactly 1 generalized branch, found $n"; exit 1; else echo "OK: exactly 1 generalized branch"; fi)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parsed.type === 'sub_agent_" frontend/src/lib/api.ts` returns `0` (zero legacy `sub_agent_*` branches — note the trailing underscore).
    - `grep -c "parsed.type === 'sub_agent'" frontend/src/lib/api.ts` returns `1` (exactly ONE generalized branch).
    - `grep -q "switch (parsed.event)" frontend/src/lib/api.ts` (the new branch dispatches on `event`).
    - `grep -q "case 'tool_start':" frontend/src/lib/api.ts && grep -q "case 'tool_done':" frontend/src/lib/api.ts` (both Explorer-specific events handled).
    - `cd frontend && npx tsc --noEmit` exits 0 (no type errors introduced — callback names match existing signatures).
    - Existing `sendMessage()` function signature is UNCHANGED (`git diff frontend/src/lib/api.ts` shows no parameter list modifications).
  </acceptance_criteria>
  <done>
    Frontend SSE consumer speaks ONLY the generalized envelope; the dual-emit window is fully closed across both producer (Task 1) and consumer (Task 2).
  </done>
</task>

</tasks>

<verification>
- Combined: `grep -c '"type": "sub_agent_' backend/app/routers/messages.py` == 0 AND `grep -c "parsed.type === 'sub_agent_" frontend/src/lib/api.ts` == 0
- Generalized envelope still in use: `grep -c '"type": "sub_agent"' backend/app/routers/messages.py` == 5 AND `grep -c "parsed.type === 'sub_agent'" frontend/src/lib/api.ts` == 1
- Backend Python parses: `cd backend && venv/Scripts/python -c "import ast; ast.parse(open('app/routers/messages.py').read())"` exits 0
- Frontend type-checks: `cd frontend && npx tsc --noEmit` exits 0
- Manual smoke (operator may run): existing chat with analyze_document still works (backwards-compat — the generalized envelope was already being emitted by the dual-emit window, so frontend just stops listening to legacy).
</verification>

<success_criteria>
The Phase 5 dual-emit window is closed in a single commit. Plan 06-07 (SubAgentSection recursive extension) can rely on ONE canonical SSE envelope shape, and Plan 06-11 Playwright tests don't have to special-case legacy events.
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-explorer-ui-cluster/06-04-SUMMARY.md` listing:
- The 5 deleted backend lines (with their final pre-deletion line numbers)
- The 5 deleted frontend branches (with their final pre-deletion line numbers)
- The 1 inserted generalized frontend branch (verbatim text)
- Confirmation grep output: 0 / 5 / 0 / 1 (counts above)
</output>
