---
phase: 06-file-explorer-ui-cluster
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/scripts/seed_admin_user.py
  - backend/migrations/021_admin_test_user.sql
  - backend/scripts/run_migrations.py
autonomous: false
requirements: [UI-11, TEST-05]
tags: [phase6, backend, admin, wave0]
user_setup:
  - service: supabase
    why: "Provision dedicated admin@test.com user for UI-11 / TEST-05 e2e Playwright suite"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Settings → API → service_role secret"
      - name: TEST_USER_ADMIN_PASSWORD
        source: "Local convention; defaults to 'adminpassword123' per backend/scripts/test_helpers.py:26-29"
must_haves:
  truths:
    - "An auth.users row exists with email='admin@test.com'"
    - "The corresponding profiles row has is_admin=true"
    - "Re-running the seed script + migration is idempotent (no duplicate users, no error)"
    - "TEST_USER_ADMIN credentials match backend/scripts/test_helpers.py convention exactly"
  artifacts:
    - path: "backend/scripts/seed_admin_user.py"
      provides: "Idempotent Python script that creates admin@test.com via supabase.auth.admin.create_user"
      contains: "supabase.auth.admin.create_user"
    - path: "backend/migrations/021_admin_test_user.sql"
      provides: "Idempotent SQL migration that promotes admin@test.com → is_admin=true"
      contains: "UPDATE profiles"
  key_links:
    - from: "backend/scripts/seed_admin_user.py"
      to: "auth.users"
      via: "supabase.auth.admin.create_user"
      pattern: "auth.admin.create_user"
    - from: "backend/migrations/021_admin_test_user.sql"
      to: "profiles"
      via: "UPDATE … SET is_admin = true WHERE email = 'admin@test.com'"
      pattern: "is_admin\\s*=\\s*true"
---

<objective>
Provision a dedicated `admin@test.com` Supabase user with `is_admin=true` so Plan 06-11's Playwright `@phase6` suite can log in as both a regular user (`test@test.com`) and an admin to assert UI-11's scope-visibility differential (admin sees "+ New Folder (Shared)" affordance; regular user does not).

Purpose: Closes D-02 (admin test account = provisioned in Phase 6). PATTERNS.md confirmed `backend/scripts/test_helpers.py:26-29` already documents the `admin@test.com` / `adminpassword123` convention; this plan automates its creation so the e2e suite is reproducible on any developer machine and on CI.
Output:
- `backend/scripts/seed_admin_user.py` — Python script using Supabase Admin API (the only way to seed an `auth.users` row with bcrypt-hashed password — pure SQL cannot do this).
- `backend/migrations/021_admin_test_user.sql` — Migration that flips `profiles.is_admin = true` for the seeded user (idempotent; safe to re-run).
- Migration registered with `run_migrations.py` so the next migration sweep applies it.
</objective>

<execution_context>
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/workflows/execute-plan.md
@C:/RAG Automators/claude-code-agentic-rag-masterclass-ep2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-file-explorer-ui-cluster/06-CONTEXT.md
@.planning/phases/06-file-explorer-ui-cluster/06-RESEARCH.md
@.planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing convention from backend/scripts/test_helpers.py:26-29 (LOCKED) -->
```py
TEST_USER_ADMIN = {
    "email": "admin@test.com",
    "password": os.environ.get("TEST_USER_ADMIN_PASSWORD", "adminpassword123"),
}
```

<!-- Existing migration ON CONFLICT idempotency pattern from migrations/005_profiles_and_settings.sql:38-50 -->
```sql
INSERT INTO public.profiles (id, email)
VALUES (NEW.id, NEW.email)
ON CONFLICT (id) DO NOTHING;
```

<!-- Service-role client pattern from backend/app/auth.py -->
SUPABASE_URL = os.getenv("SUPABASE_URL")
SUPABASE_SERVICE_ROLE_KEY = os.getenv("SUPABASE_SERVICE_ROLE_KEY")
client = create_client(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false" id="06-02-01">
  <name>Task 1: Create idempotent seed_admin_user.py</name>
  <files>backend/scripts/seed_admin_user.py</files>
  <read_first>
    - backend/scripts/test_helpers.py (lines 1-60 — verify the exact TEST_USER_ADMIN constant + password env var name)
    - backend/app/auth.py (service-role Supabase client construction pattern)
    - backend/scripts/run_migrations.py (existing service-role client usage pattern)
    - .planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md (section "backend/migrations/021_admin_test_user.sql" at lines 488-516 — explanation of why pure SQL cannot seed auth.users)
  </read_first>
  <action>
Create a new file `backend/scripts/seed_admin_user.py` that:

1. Loads env vars: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (REQUIRED — exit with clear error if missing); `TEST_USER_ADMIN_PASSWORD` (optional, defaults to `"adminpassword123"` to match `backend/scripts/test_helpers.py:26-29`).
2. Constructs a service-role Supabase client (re-uses the same pattern as `backend/app/auth.py` and `backend/scripts/run_migrations.py`).
3. Calls `supabase.auth.admin.create_user({"email": "admin@test.com", "password": <password>, "email_confirm": True})` — `email_confirm=True` skips the confirmation email, important for test users (per Supabase Admin API docs).
4. Handles the "user already exists" case idempotently: catch the `AuthApiError` with `User already registered` message → log and treat as success. (If the supabase-py exception class differs, use `try/except Exception as e` and check `"already" in str(e).lower()`.)
5. After successful create-or-detect-existing, look up the user via `supabase.auth.admin.list_users()` (or `get_user_by_email` if available in the version pinned in `requirements.txt`), capture the `user.id`, and print it as `Admin user UUID: <uuid>` so the operator can verify.
6. Print `OK: admin@test.com is provisioned (is_admin promotion happens in migration 021)` on success.
7. Exit code 0 on success, 1 on any unrecoverable error (missing env, network failure, etc.).

Do NOT attempt to set `is_admin` from this script — that is the migration's job (separation of concerns: seed creates the auth row, migration promotes the profile). Do NOT delete or update an existing user with the same email — only create-if-missing.

Add a CLI block: `if __name__ == "__main__": main()` so the script can be run via `cd backend && venv/Scripts/python scripts/seed_admin_user.py`.

Top-of-file docstring must explain: "Per D-02 (06-CONTEXT.md): provisions admin@test.com for the Phase 6 Playwright @phase6 suite. Idempotent. Companion to migration 021 which sets is_admin=true on the resulting profile row."
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -c "import importlib.util, pathlib; spec = importlib.util.spec_from_file_location('seed', pathlib.Path('scripts/seed_admin_user.py')); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('module loads OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/scripts/seed_admin_user.py` (file exists).
    - `grep -q "supabase.auth.admin.create_user" backend/scripts/seed_admin_user.py` (uses Admin API per D-02 / PATTERNS.md "Supabase Auth users CANNOT be seeded purely via SQL").
    - `grep -q "admin@test.com" backend/scripts/seed_admin_user.py` (hardcoded email matches test_helpers.py convention).
    - `grep -q "TEST_USER_ADMIN_PASSWORD" backend/scripts/seed_admin_user.py` (env var name matches existing convention).
    - `grep -q "email_confirm" backend/scripts/seed_admin_user.py` (skips confirmation email for test usability).
    - `grep -qE "(already|exists)" backend/scripts/seed_admin_user.py` (idempotency handler present).
    - Module loads without ImportError when invoked via Python (verify command above).
  </acceptance_criteria>
  <done>
    `seed_admin_user.py` exists, imports cleanly, and when run with valid `SUPABASE_SERVICE_ROLE_KEY` either creates `admin@test.com` (first run) or detects-and-skips (subsequent runs) without raising.
  </done>
</task>

<task type="auto" tdd="false" id="06-02-02">
  <name>Task 2: Create migration 021_admin_test_user.sql + register</name>
  <files>backend/migrations/021_admin_test_user.sql, backend/scripts/run_migrations.py</files>
  <read_first>
    - backend/migrations/005_profiles_and_settings.sql (lines 1-60 — ON CONFLICT DO NOTHING idempotency pattern; profiles table shape)
    - backend/scripts/run_migrations.py (lines 1-100 — find the migration list / sweep pattern; identify exactly where to add the new migration filename)
    - .planning/phases/06-file-explorer-ui-cluster/06-PATTERNS.md (lines 488-516 — both halves of the seed-script vs migration split)
  </read_first>
  <action>
Step A — create `backend/migrations/021_admin_test_user.sql`:

```sql
-- Migration 021: Admin test user promotion (Phase 6 / D-02)
-- COMPANION to backend/scripts/seed_admin_user.py — that script must run FIRST to create
-- the auth.users row (Supabase Admin API; bcrypt-hashed password). This migration only
-- handles the profile-level promotion, which IS expressible in pure SQL.
--
-- IDEMPOTENT: re-running this migration is a no-op once is_admin is already true.

DO $$
DECLARE
    v_user_id UUID;
BEGIN
    -- Look up the seeded admin user
    SELECT id INTO v_user_id
    FROM auth.users
    WHERE email = 'admin@test.com'
    LIMIT 1;

    IF v_user_id IS NULL THEN
        RAISE EXCEPTION 'Migration 021: admin@test.com not found in auth.users. Run `cd backend && venv/Scripts/python scripts/seed_admin_user.py` FIRST, then re-apply migrations.';
    END IF;

    -- Promote to admin (idempotent — safe to re-run)
    UPDATE public.profiles
    SET is_admin = true
    WHERE id = v_user_id;

    -- If the profile row doesn't exist yet (handle_new_user trigger may not have fired in some test environments), insert it
    INSERT INTO public.profiles (id, email, is_admin)
    VALUES (v_user_id, 'admin@test.com', true)
    ON CONFLICT (id) DO UPDATE SET is_admin = true;

    RAISE NOTICE 'Migration 021: admin@test.com (id=%) promoted to is_admin=true', v_user_id;
END $$;
```

Step B — register the migration in `backend/scripts/run_migrations.py`. Read the file first to find the migration list/array (typically a sorted glob or an explicit list around the top of the file). Two cases:
  - If migrations are auto-discovered via `glob("backend/migrations/*.sql")`: no code change needed; the new file is picked up automatically. Verify by reading the discovery block.
  - If migrations are listed explicitly in an array: append `"021_admin_test_user.sql"` to the list AT THE END (preserves numeric order).

Document which case applied in the SUMMARY.

Do NOT add the seed-script invocation to `run_migrations.py` — they are separate concerns (operator runs the seed script once before applying migrations on a fresh environment). The migration's `RAISE EXCEPTION` is the safety net.
  </action>
  <verify>
    <automated>test -f backend/migrations/021_admin_test_user.sql && grep -q "is_admin" backend/migrations/021_admin_test_user.sql && grep -q "admin@test.com" backend/migrations/021_admin_test_user.sql && echo "OK: migration 021 present with required tokens"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/migrations/021_admin_test_user.sql`.
    - `grep -q "UPDATE public.profiles" backend/migrations/021_admin_test_user.sql` (promotion statement present).
    - `grep -q "is_admin = true" backend/migrations/021_admin_test_user.sql` (sets the admin flag).
    - `grep -q "ON CONFLICT" backend/migrations/021_admin_test_user.sql` (idempotency guard).
    - `grep -q "admin@test.com" backend/migrations/021_admin_test_user.sql` (hardcoded email matches D-02).
    - `grep -q "RAISE EXCEPTION" backend/migrations/021_admin_test_user.sql` (failure-loud safety net when seed script wasn't run).
    - The migration is reachable from `run_migrations.py` (either via glob auto-discovery — verify by reading file — or explicit list registration).
  </acceptance_criteria>
  <done>
    Migration 021 file exists, promotes `admin@test.com` to `is_admin=true` idempotently, raises a clear error if the seed script hasn't been run yet, and is picked up by `run_migrations.py`.
  </done>
</task>

<task type="checkpoint:human-action" id="06-02-03" gate="blocking">
  <name>Task 3: Operator runs seed script + applies migration</name>
  <what-built>
    Plan 06-02 Tasks 1+2 created the seed script and migration. The seed script REQUIRES the service-role key, which is a secret that lives only in the operator's `.env` / shell environment — Claude cannot run it without that secret being available in the execution environment.
  </what-built>
  <how-to-verify>
    Operator runs the following two commands (in order) and confirms green output:

    1. `cd backend && venv/Scripts/python scripts/seed_admin_user.py`
       → Expected: `OK: admin@test.com is provisioned (is_admin promotion happens in migration 021)` and a UUID printed. On subsequent runs: idempotent log line and exit 0.
    2. `cd backend && venv/Scripts/python scripts/run_migrations.py`
       → Expected: migration `021_admin_test_user.sql` runs and emits the NOTICE line `Migration 021: admin@test.com (id=&lt;uuid&gt;) promoted to is_admin=true`.
    3. Optional sanity check via Supabase SQL editor: `SELECT email, is_admin FROM profiles WHERE email='admin@test.com';` returns one row with `is_admin = true`.
  </how-to-verify>
  <resume-signal>Type "approved" with the printed UUID, or paste the error output if either command failed.</resume-signal>
</task>

</tasks>

<verification>
- `test -f backend/scripts/seed_admin_user.py && test -f backend/migrations/021_admin_test_user.sql`
- After operator runs Task 3: `admin@test.com` exists in `auth.users` and `profiles.is_admin = true` for that user
- `backend/scripts/test_helpers.py:26-29` `TEST_USER_ADMIN` constants are unchanged (the seed script consumes the same convention; it does not redefine it)
</verification>

<success_criteria>
The Phase 6 Playwright `@phase6` suite (Plan 06-11) can call a `signInAdmin(page)` helper that uses the seeded credentials and lands on a session where `useAuth().isAdmin === true`, enabling the UI-11 scope-visibility differential test.
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-explorer-ui-cluster/06-02-SUMMARY.md` documenting:
- The seed script's behavior (idempotency, env vars consumed)
- The migration's role split from the seed script
- The operator's confirmation output from Task 3
- Whether `run_migrations.py` discovers via glob or explicit list (Step B finding)
</output>
